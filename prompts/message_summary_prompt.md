Te pasaré un bloque de transcripción que corresponde al **Mensaje** (desde "Mensaje" hasta antes de "Ministración").

Quiero un resumen en puntos:
- En español.
- Con enfoque práctico y claro.
- Cada punto debe iniciar con un emoji relevante.
- Hazlo extendido: minimo 5 puntos (sin limite maximo estricto, segun contenido).
- Cada punto con suficiente detalle (idealmente 1-2 frases breves).
- Si en ese punto se menciona un versiculo/pasaje, incluye la referencia exacta al final entre parentesis.
- No omitas citas biblicas explicitas del mensaje; prioriza preservarlas.
- No inventes referencias biblicas que no aparezcan en la transcripcion.
- No inventes información; usa solo lo que aparece en la transcripción.
- Evita relleno y frases repetidas.

**Formato de salida obligatorio**.
Responde en JSON con estructura `{"summary_points": ["emoji + punto", "..."]}`.
No incluyas markdown, explicaciones ni texto fuera del JSON.

JSON Schema:
```json
<SUMMARY_JSON_SCHEMA>
```

"""
<TRANSCRIPCION_MENSAJE>
"""
